name: C/C++ CI

on:
  push:
    branches: 'master'
  pull_request:
    branches: '*'
    
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get -y install git gcc-multilib gcc g++ autoconf pkg-config make automake libxml2-dev libxml2 python3 python3-dev python3-pip python3-setuptools openssl libssl-dev
    - name: autoreconf
      run: autoreconf -i
    - name: configure
      run: ./configure -q --enable-debug CXXFLAGS=-coverage CFLAGS=-coverage LDFLAGS=-lgcov --prefix=/usr
    - name: make
      run: make -j10
    - name: install
      run: sudo make install
    - name: install pytrap
      run: (cd pytrap && make coverage; sudo python3 setup.py install;)
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
    - name: upload to codecov
      run: bash <(curl -s https://codecov.io/bash)

